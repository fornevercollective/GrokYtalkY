# edge — public audience plane

| Path | Role |
|------|------|
| [`mid-lane/`](mid-lane/) | **Reference mid-lane worker** — program + hexlum fan-out (pairs with `gy mid-lane`) |
| [`../chat/worker/`](../chat/worker/) | Public Space chat (separate plane) |

Interactive DOJO stays on **`gy serve` rooms** + **`sfu/`**.  
This directory is for **1k+ viewers**, not jam peers.

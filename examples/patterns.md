# Pattern examples (Strudel-lite)

```
s("bd sd hh cp")
s("bd*4, [~ sd]*2")
setcps(0.6)
stack(s("bd*4"), note("c2 g2"))
bpm(124)
note("c4 e4 g4 c5")
```

Enter in **live** mode (`tab` or `l`). Press `p` to play/stop.
Local synth plays via afplay/ffplay; MIDI also emits on port `GrokYtalkY`.
